<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="UTF-8">
<title>answered</title>

<link rel="stylesheet" href="https://
maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/
bootstrap.min.css">
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/
3.3.5/js/bootstrap.min.js"></script>
</head>
<body>
<div class="row">
<div class="col-lg-8 col-lg-offset-2">
<div class="panel panel-default">
<div class="panel-heading">
    <b>回答一覧</b>
    </div>
<div class="panel-body">
<table class="table">
    <tr>
        <th>名前</th>
        <th>メール</th>
        <th>タイトル</th>
        <th>フィードバックの種類</th>
        <th>コメント</th>
        <th>OS</th>
        <th>デバイス</th>
        <th>使用目的</th>
        <th>削除</th>
        <% @answers.each do |answer| %>
    </tr>
    <tr>
        <td><%= answer.name %></td>
        <td><%= answer.mail %></td>
        <td><%= answer.topic %></td>
        <td><%= answer.product %></td>
        <td><%= answer.feedback_type %></td>
        <td><%= answer.comment %></td>
        <td><%= answer.os %></td>
        <td><%= answer.device %></td>
        <td><%= answer.purpose %></td>
        <td>
            <form action="/delete/<%= answer.id %>" method="post">
                <input type="submit" class="btn btn-primary pull-left" value="削除"></input>
            </form>
        </td>
    </tr>
    <% end %>
</table>
</div>
</div>
</div>
</div>
<div align="right">
    <a href="/">トップへ戻る</a>
</div>
</body>
</html>